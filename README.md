# Java-String-Substring-Example
A simple Java program that demonstrates how to extract specific characters from a string using the substring() method. It also shows how to safely handle shorter strings using Math.min() to avoid errors.
